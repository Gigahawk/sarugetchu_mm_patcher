#!/usr/bin/env bash

# そうびへんこう (装備変更)
# Slightly lower font size and adjust positioning to
# avoid issues with dangling tail of 'Q'
../build_header.sh \
    -s "MODIFY EQUIPMENT" \
    --font-size 35 \
    -b 4 \
    --outline mainmenu
